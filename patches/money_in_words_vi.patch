--- /tmp/data.py.orig	2026-04-17 00:34:53.491042668 +0700
+++ /tmp/data.py.patched	2026-04-17 00:34:52.000000000 +0700
@@ -1561,6 +1561,12 @@
 		if main_currency == "DZD":
 			# Use Dinars for Algerian Compliance
 			out = in_words(main, in_million).title() + " " + _("Dinars", context="Currency")
+		elif (getattr(frappe.local, 'lang', None) or '').startswith('vi'):
+			# Vietnamese convention: first word capitalized only; currency word at end.
+			curr_word = _(main_currency, context="Currency")
+			words = in_words(main, in_million)
+			words = words[:1].upper() + words[1:] if words else words
+			out = words + ((" " + curr_word) if curr_word and curr_word != main_currency else "")
 		else:
 			out = _(main_currency, context="Currency") + " " + in_words(main, in_million).title()
 		if cint(fraction):
